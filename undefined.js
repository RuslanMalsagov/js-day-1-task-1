// Значение глобального свойства undefined представляет значение undefined. Это одно из примитивных значений JavaScript.

const nothing = (sum) => (sum === undefined ? "Ничего" : sum);

console.log(nothing());

// Ничего в голову не пришло
let nothing_1 = undefined;

console.log(nothing_1);

let inf;

console.log(inf);
